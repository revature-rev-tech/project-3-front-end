import { ProductAndDiscount } from './product-and-discount.model';

describe('ProductAndDiscount', () => {
  it('should create an instance', () => {
    expect(new ProductAndDiscount()).toBeTruthy();
  });
});
